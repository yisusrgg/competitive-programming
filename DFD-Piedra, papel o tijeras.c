#include <stdio.h>
int main()
{
    int a, b;
    scanf("%d %d", &a, &b);
    if(a==0 && b==2)
    {
        else if(a==2 && b==0)
        {
            printf("MARTONTITO");
        }
        printf("FLIX");
    }
    return 0;
}
