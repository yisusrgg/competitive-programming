#include <stdio.h>

int main ()
{
    double a1, b1, c1, a2, b2, c2, x, y;
    scanf("%lf %lf %lf %lf %lf %lf", &a1, &b1, &c1, &a2, &b2, &c2);

    y = (c1 - c2 * a1) / (b1);
    x =

    printf("%lf %lf", x, y);

    return 0;
}
